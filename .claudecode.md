# Universal Polyglot Refactoring & Architectural Guidelines

You are an expert Principal Software Architect. Your objective is to refactor this codebase to maximize modularity, readability, and testability while maintaining strict functional correctness and zero regressions.

## 🏛️ Core Architectural Philosophies (Language-Agnostic)
- **Clean Architecture & The Dependency Rule:** Business rules and core execution logic must be isolated from external frameworks, databases, network protocols, and side effects. Dependencies must only point inward toward core business intent. Use the Ports and Adapters (Hexagonal) pattern to abstract I/O.
- **Information Hiding (Deep Modules):** Expose simple, minimal external interfaces. Hide complex implementation details internally. Prioritize locality of behavior over deeply nested, highly fragmented micro-files that lead to tight coupling.
- **Contract-Driven Design:** Before refactoring any scope, explicitly identify or define the data types, schemas, inputs, and outputs crossing that boundary. Ensure those contracts are strictly enforced post-refactor.

## 🔧 Technology-Specific Guardrails

### 1. Smart Contracts & Web3 (Solidity, Foundry, Hardhat)
- **Security & Efficiency:** Adhere strictly to the ConsenSys Solidity Best Practices. Keep functions short, optimize gas costs (e.g., caching storage variables in memory), and strictly check access controls (`onlyOwner`, custom modifiers).
- **State Modifications:** Enforce the Checks-Effects-Interactions pattern globally to prevent reentrancy vulnerabilities.
- **Tooling Verification:** Every architectural shift or optimization must be compiled and validated using `forge test` or `npx hardhat test`. Ensure fuzzing or invariant tests do not break.

### 2. Frontend & Full-Stack Ecosystems (TypeScript, React, Next.js, Vue)
- **Separation of Concerns:** Separate pure UI rendering logic from data fetching, state management, and business hooks.
- **Type Rigor:** Eliminate all instances of `any`. Utilize strict interfaces, generic type parameters, and schema validation libraries (like Zod or ArkType) to parse untrusted network I/O or form inputs.
- **State Locality:** Keep state as local as possible to avoid unnecessary global context re-renders.

### 3. Backend Services & APIs (Node.js, Python, Go, Java)
- **Statelessness & Scalability:** Keep application layers stateless. Offload state to external databases or caches.
- **Robust Error Handling:** Never swallow exceptions. Implement centralized middleware for error logging and map internal execution errors cleanly to HTTP/gRPC status codes.
- **Boundary Validation:** Enforce structural type hints or data models (e.g., Pydantic in Python, NestJS DTOs) at the controller/gateway boundary.

### 4. Infrastructure & Scripting (Bash, Shell, Python Scripts)
- **Idempotency & Safety:** Shell scripts must execute safely using `set -euo pipefail`. Localize variable scopes explicitly with `local`.
- **Modularity:** Break down monolithic automation scripts into testable, discrete functions. Validate syntax and anti-patterns against tools like `shellcheck`.

## 🔄 Verification, Linting, & Quality Gate Loop
When asked to perform a refactor, you must utilize your local terminal access to execute this strict continuous-verification loop:
1. **Establish Baseline:** Execute the project's linter, compiler, and test runner before modifying any code to ensure a passing baseline.
2. **Incremental Scoping:** Refactor in small, atomic batches. Do not alter more than 150 lines of code across any given file in a single step.
3. **Automated Verification:** Run local verification tools (e.g., `forge test`, `npm run lint`, `pytest`, `tsc --noEmit`, `shellcheck`) after every single atomic change.
4. **Automated Rollback:** If a verification gate fails and a clean patch cannot be resolved immediately, use git tools to revert the changes to that specific file and attempt a different, safer structural pattern.

---

## Project contracts

Measured 2026-08-29 at 8f2b81c, on this machine.
Every value came from a command actually run; what could not be established says `unknown`.

```
Verification
  behaviour: npm test          # vitest, exit 0 - 40 files, 903 tests, 0 skipped here
    covers:  all 38 modules under server/ - the tests import every one by name, so each
             pillar reader is exercised directly.
    blind:   the UI and the HTTP layer. Of 34 files in src/ only sortable.tsx is imported by
             a test; every view, App.tsx, api.ts and markdown.ts are unreached. The one thing
             that renders them is gate check 11 (Playwright, not in CI), which asserts only a
             .view-title, >=1 node matching a coarse selector (.card, or one table row) and no
             console errors - a view cut down to its heading plus one row still passes.
             server/index.ts declares 55 routes; 9 are driven through the Hono app (health, a
             404, wraps, index/stats, search, five launch paths), the other ~46 are proven as
             module calls, never as responses. With no linter and no noUnusedLocals, an import
             orphaned by an extraction is invisible to every gate, a fully green one included.
  shape:     npm run typecheck # tsc --noEmit, exit 0
             npm run build     # vite build, exit 0
  gate:      npm run gate - the acceptance check, 17 checks, roster at the top of gate.mjs.
             NOT RUN, exit code unknown: check 1 is `npm ci`, which wipes and reinstalls
             node_modules, and the vitest hang guard alone is 480s. The commands above are its
             data-independent half; ci.yml and CLAUDE.md both say green CI is not acceptance.
  data:      npm run doctor, exit 0 - 16/17 sources present here (pacingLogPath absent). Six
             suites are describe.skipIf on a real source, so this machine runs strictly more
             tests than a stranger's clone will.
  sensitivity: none - green means "did not object". No mutation harness or planted-defect
             self-test exists anywhere in the tree. gate-budgets.test.ts and script-config-
             parity.test.ts assert a non-empty corpus before comparing, the only anti-vacuity
             control here; CLAUDE.md records check 12 printing PASS while observing nothing.

Provenance
  authored:  every tracked file (126) but package-lock.json. Nothing in-tree is generated or
             vendored, and `git ls-files -s` finds no tracked symlink.
  generated: package-lock.json by npm, dist/ by npm run build, .cache/index.db by the server
             on demand (check 10d asserts deleting it loses nothing). The last two are
             gitignored, with config.json, .claude/ and docs/superpowers/. CI's `npm ci` is
             the only thing verifying the lock against package.json.
  intentional: config.example.json ships <placeholder> path segments - substituting real ones
             is a PII leak, not a tidy-up. Source-path defaults are triplicated across
             server/config.ts, gate.mjs and doctor.mjs so both scripts stay zero-dependency
             (check 1 wipes node_modules); script-config-parity.test.ts keeps them equal and
             DRY-ing them breaks the gate. Gate checks are numbered 1-13 plus 3b/10b/10c/10d,
             non-sequential so references elsewhere stay valid - do not renumber.
  liveness:  nothing consumes this tree - no listener on 4317, no matching process, no launchd
             job, no symlink from ~/.claude into it, all four checked. The coupling runs the
             other way: suite and gate read the operator's live ~/.claude, engram vault,
             friction log and token-analyzer database.

Authority
  1. npm run gate - CLAUDE.md: "the mechanical definition of 'working.'"
  2. the ROSTER at the top of scripts/gate.mjs, over any prose count of checks
  3. CLAUDE.md, "Invariants - do not break these"
  4. ci.yml, the data-independent half; green there is explicitly not an acceptance claim
  5. README.md explains, never overrides. This file loads via @.claudecode.md at the end of
     CLAUDE.md, so it is read but ranks below the invariants.

Boundary
  HTTP seam: server/index.ts (Hono) <-> src/api.ts, untyped across the wire. apiGet<T> asserts
    T and each view re-declares the response shape by hand; no shared types module exists, so
    tsc cannot catch a server shape change. Change both sides together.
  Source-missing seam: server answers 503 {error, pillar, path}, src/api.ts raises
    SourceMissing, the view renders .not-configured - never an error panel.
  Parity-enforced: the source-path defaults in the three files above, and the pillar route set
    named in src/App.tsx, scripts/ui-smoke.mjs and scripts/gate.mjs - both compared by
    tests/script-config-parity.test.ts, so skipping one goes red rather than silent.
  new modules: a pillar is server/<name>.ts + src/views/<Name>View.tsx + tests/<name>.test.ts,
    flat; server/text/ is the only nested group. Pairing is not strictly 1:1 - some readers
    share a suite (graph-pacing-history, skills-launch, text-*).
  Fails silently if skipped: writing tests/<name>.test.ts at all. Nothing measures coverage,
    so a new server module with no test leaves every gate green.
```
